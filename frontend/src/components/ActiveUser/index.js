// 3rd party imports
import React from 'react'

// My imports

const ActiveUser = ({ username, avatar }) => {
  return (
    <div>
      username: {username}, avatar: {avatar}
    </div>
  )
}

export default ActiveUser

// STYLING
